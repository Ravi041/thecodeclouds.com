{
  "name": "the-code-clouds",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "dev": "eleventy --serve --port=8088",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "test:browser": "node scripts/browser-check.mjs",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "@axe-core/playwright": "4.13.0",
    "playwright": "1.63.0"
  }
}
